# ---- InfraMind OPTIONAL cloud-connector dependencies ----
# SDKs for pulling LIVE data from real AWS and Azure subscriptions.
# Without these, every connector runs in "mock" mode using bundled sample data,
# so the platform is fully functional with zero cloud setup.
#
# Install with:  pip install -r requirements.txt -r requirements-cloud.txt
# Then open the Setup page (or set <connector>_MODE=live in .env) to go live.

# --- AWS ---
boto3>=1.34,<2

# --- Azure ---
azure-identity>=1.16,<2
azure-mgmt-resource>=23.0,<24
azure-mgmt-costmanagement>=4.0,<5
azure-mgmt-monitor>=6.0,<7
azure-mgmt-security>=6.0,<8
